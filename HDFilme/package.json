{
  "name": "hdfilme",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild plugin.js --bundle --outfile=.build/plugin.bundled.js --target=es2020"
  },
  "dependencies": {
    "skystream-extractors": "^1.3.7"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}